Language: Detect the language of the task ({{TASK}}) and use that exact language for all output.

IMPORTANT: The output is printed to the console (terminal). Avoid using markdown features such as markdown headers (e.g. ##), tables, lists with asterisks (*), or emojis. Use only plain text, numbered lists, and space indentations. Make sure everything you write is easily readable in a monospaced terminal.

{{include:common_style}}

STRICTLY FORBIDDEN:
1. Do not use any HTML entities in the text (such as &nbsp;, &lt;, &gt;, &amp;, &quot;, &apos;, etc.). Instead, use ordinary spaces, symbols like <, >, &, and regular quotes.
2. Do not use any emojis, graphical characters, symbols, or emoticons. The output must be strictly plain text.
