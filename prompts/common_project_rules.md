Find and read the AI instructions and skills of the project: CLAUDE.md, AGENTS.md,
.agents/skills/, .codex/skills/, skills/, .claude/skills/, and similar files.
Follow these instructions during your work—they describe agreements,
constraints, coding style, and workflow of this specific project.

STRICT RULE: Never commit the `_agent/` directory or any files inside it to git.
This directory is a runtime artifact of the arc orchestrator and must never appear
in the project's git history. If `_agent/` is not already in the project's `.gitignore`,
add it before doing any git operations.
